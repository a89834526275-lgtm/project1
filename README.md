# Проект 1

## Описание:

Проект 1 - это виджеты на Python для управления задачами банковских операций.

## Установка:

1. Клонируйте репозиторий:
```
git clone https://github.com/a89834526275-lgtm/project1
```
2. Установите зависимости:
```
pip install -r requirements.txt
```
## Содержание: 

### Модуль masks содержит две функции:
```
1. **get_mask_card_number -** Функцию маскировки номера банковской карты 

2. **get_mask_account -** Функцию маскировки номера банковского счета 

```
### Модуль widget содержит две функции:

1. **mask_account_card -** Функция обрабатывает информацию как о картах, так и о счетах.

2. **get_date -** Функция изменяет формат даты 


### Модуль processing содержит две функции:

1. **filter_by_state -** Функция принимает список словарей и опциональное значение для ключа 
state
 (по умолчанию 
'EXECUTED'
). Функция возвращает новый список словарей, содержащий только те словари, у которых ключ 
state
 соответствует указанному значению.

2. **sort_by_date -** Функция принимает список словарей и необязательный параметр(*True* или *False*), задающий порядок сортировки (по умолчанию — убывание). Функция должна возвращать новый список, отсортированный по дате (
date
).


### Модуль generators содержит три функции:

1. **filter_by_currency -** Функция возвращает итератор, который поочередно выдает транзакции, где валюта операции соответствует заданной.
2. **stransaction_descriptions -** Функция возвращает описание каждой операции по очереди.
3. **card_number_generator -** Генератор может сгенерировать номера карт в заданном диапазоне от 0000 0000 0000 0001 до 9999 9999 9999 9999.


### Тестирование

в проекте предусмотренно тестирование для функций 
вызов теста произвести через команду 
````
pytest
````
